apps:
  - script: ./dist/index.js
    name: "apollo-app-cluster"
    watch: false
    instances: 4
    exec_mode: cluster
    env:
      NODE_ENV: production
    env_production:
      NODE_ENV: production
